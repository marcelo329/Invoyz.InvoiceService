# Defaults for every environment. Override per environment in .env.development /
# .env.production, or locally in .env.local (gitignored).
VITE_API_BASE_URL=/api/v1
VITE_API_TIMEOUT_MS=15000
VITE_DEFAULT_PAGE_SIZE=10
VITE_APP_TITLE=Invoyz
VITE_INVOICE_STATUSES=Draft,Sent,Paid,Overdue
