# Dev goes through the Vite proxy (see vite.config.ts) so the browser never makes a
# cross-origin call: the API has no CORS policy.
VITE_API_BASE_URL=/api/v1
VITE_API_PROXY_TARGET=https://localhost:7280
